# Platter

Semantics-Project: cell

- Keep this requester simple. Cast owns discovery; Annals retains Vita career material.
  Nucleus owns execution; Platter owns preparation and delivery state.
- Until a separate semantic repository is registered, query Cell terminology.
- New runs replace only Jackson, Cell and Wrought bullet regions in the captured
  fixed template. Weaver owns project prose; Platter's model authors only the
  brief and Jackson. Historical runs keep their original boundary and tools.
  Never commit the user's private resume or profile.
- Keep accepted stages and exact send occurrences durable across failures.
- Submit committed code changes through `./ci.sh submit COMMIT` from the Cell
  root and verify the manager job outcome. Follow the root CI instructions.
