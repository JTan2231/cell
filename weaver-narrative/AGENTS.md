# Weaver

Semantics-Project: weaver-narrative

- Read this project's installed Semantics repository before analysis or changes.
  It owns maintained terminology. Source, tests, and contracts define behavior.
- Keep one free-form direction, one Nucleus authoring job, and Markdown output.
  Put editorial choices in the prompt. Do not add citation or source history records.
- Annals owns source reading. Nucleus owns execution and authentication.
  Weaver owns saved documents and the minimum execution recovery state.
- Use SQLite and the shared Cell installation and maintenance interfaces.
- Run the default root `./ci.sh` before deployment.
