{
  "schema": 1,
  "product": "semantics",
  "application": "Semantics",
  "dependencies": [
    "annals",
    "nucleus",
    "clockwork",
    "conversations"
  ],
  "description": "Deploy Semantics with owned admission, retained project pauses, exact worker control and dependency readiness.",
  "adapter_binary": "semantics-install",
  "maintenance_products": [
    "nucleus"
  ]
}
