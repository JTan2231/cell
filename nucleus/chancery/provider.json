{
  "schema_version": 3,
  "provider": {
    "id": "nucleus",
    "name": "Nucleus",
    "release": "0.5.5"
  },
  "promise_scope": {
    "authoritative_for": [
      "Nucleus owns version-one invocation admission, the global eight-slot scheduler, and one supervised attempt per job. It also owns job and cancellation lifecycle, exact non-authentication output atoms, immutable schemas and toolsets, the durable requester-tool mailbox, and the managed authentication broker with one authoritative credential.",
      "Nucleus SQLite is authoritative for its operational job, attempt, registration, cancellation, mailbox, and output records; the installed service owns its per-user socket, private Codex home, and service lifecycle."
    ],
    "not_authoritative_for": [
      "Each requester remains authoritative for the domain state, domain success, idempotent mutation, duplicate handling, and whether a new attempt is safe.",
      "Codex owns harness and account behavior. Requesters own their domain records and the operations that commit them."
    ],
    "inventory": {
      "covers": [
        "All supported public Nucleus version-one CLI and HTTP execution and service outcomes, requester-integration operations, and Nucleus development operations in this release."
      ],
      "completeness": "complete",
      "excludes": [
        "Help and version display, implementation primitives, requester-owned workflows and domain operations, direct SQLite access, unsupported harnesses, current live readiness, and Codex behavior outside the proved adapter contract are outside this inventory claim."
      ]
    },
    "shared_access_and_trust": [
      "The standard runtime is a current-user local service over a filesystem-protected Unix socket with no application-level authentication or TCP listener.",
      "Installed discovery grants no socket, credential, workspace, tool, service-control, or retry authority; each entry declares its narrower invocation and effect boundary."
    ],
    "shared_privacy_and_retention": [
      "Nucleus private state may contain complete prompts, tool arguments and results, source content, exact harness output, terminal diagnostics, and authentication material.",
      "Nucleus promises no automatic pruning policy for retained job or output records; provider documentation must contain no credentials, private runtime records, or requester source bodies."
    ],
    "compatibility_and_retirement": [
      "Provider release, entry contract, invocation protocol, store schema, adapter, harness compatibility, and immutable schema or toolset identity have independent compatibility rules.",
      "Incompatible public meanings require a new version and retained historical decoding; no broader compatibility window or deprecation period is promised unless an entry states one."
    ],
    "operational_limits": [
      "Version one admits one attempt per job and at most eight active attempts globally. Accepted jobs wait for a slot; attempts waiting on requesters keep their slots. Nucleus has no automatic retry, workflow graph, project registry, capability directory, or requester-domain interpretation. Durable maintenance holds belong to run IDs. They block new admission and report job drain; requesters decide workflow recovery.",
      "Account reads may overlap active jobs. The managed authentication broker keeps one authoritative private Codex home, serializes staged atomic refresh through worker cancellation, and excludes attended login until active job and account sessions have ended; authentication remains forward-only across program, database, and service rollback.",
      "Live acceptance remains bounded by installed service, credential, adapter, account, local storage, and exact harness readiness, none of which provider installation proves."
    ]
  },
  "entries": [
    "entries/execution-operate.json",
    "entries/requester-integrate.json",
    "entries/develop-change.json"
  ]
}
